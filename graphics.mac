
; Sets the color of a pixel at an X,Y (column, row) location.
; Clobbers ax, bx, dl.
; %1: X (row)
; %2: Y (column)
; %3: color (4-bit pallete index)
%macro setpixel 3
  mov bx, %1
  mov ax, %2
  mov dl, %3
  call putpixel
%endmacro
